import 'package:flutter/material.dart';

class LessonChartPage extends StatelessWidget {
  const LessonChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Text("This is Lesson Chart page"),
      ),
    );
  }
}
